# ForgeAI — Environment Configuration
# Copy this file to .env and fill in the values

# ─── MySQL ───────────────────────────────────────────
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=
MYSQL_DATABASE=forgeai

# ─── Gateway ─────────────────────────────────────────
GATEWAY_HOST=127.0.0.1
GATEWAY_PORT=18800
GATEWAY_WS_PORT=18801
GATEWAY_SECRET=change-me-to-a-random-secret

# ─── Security ────────────────────────────────────────
VAULT_MASTER_PASSWORD=change-me-to-a-strong-password
JWT_SECRET=change-me-to-a-random-jwt-secret
JWT_EXPIRES_IN=24h
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=60

# ─── STT/TTS (Voice) ─────────────────────────────────────────────
# VPS-hosted Whisper (STT) and Piper (TTS) - free, no OpenAI credits
STT_TTS_API_KEY=your-stt-tts-api-key
# Optional: override default VPS URL (default: http://167.86.85.73:5051)
# WHISPER_API_URL=http://your-vps:5051
# PIPER_API_URL=http://your-vps:5051

# ─── Node Protocol (IoT/Embedded) ───────────────────────────────
# Managed via Dashboard → Settings (recommended). Generate a secure key there.
# Only use .env as fallback — Dashboard keys are encrypted in Vault (AES-256-GCM).
# NODE_API_KEY=your-node-protocol-key

# ─── LLM Providers, Channels & Permissions ──────────────────────
# API keys, channel tokens, and channel permissions are ALL managed via the Dashboard.
# They are stored encrypted (AES-256-GCM) in .forgeai/vault.json — NOT in this file.
# Supported channels: Telegram, WhatsApp, Discord, Slack, Microsoft Teams, WebChat
